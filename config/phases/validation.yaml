# Validation phase settings
# Vertical comes from runtime_config (set via begin_session).
# Optional: filter to a specific validation category (e.g., 'financial', 'data_quality')
# validation_category: financial
